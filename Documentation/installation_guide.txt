For this project, the only module that needs to be installed is pygame. Because pygame is only compatible with python2, this pacman game is not python3 compatible.

Other that that modules used in this project are os, sys, math, and pickle. These are all in the standard distribution of python. 
